<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CoutureAI - AI Fashion Generator</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <div class="container">
        <!-- Sidebar Section -->
        <aside class="sidebar">
            <h1>CoutureAI</h1>
            <p>Create an image from text prompt</p>

            <textarea id="description" placeholder="Enter your clothing idea..." rows="4"></textarea>
            <button id="generateBtn">Generate</button>

            <div class="loader-container" id="loaderContainer">
                <div class="loader"></div>
                <p>&nbsp;Generating image...</p>
            </div>
        </aside>

        <!-- Image Display Section -->
        <main class="image-section">
            <div id="imageContainer">
                <p>No image generated yet</p>
            </div>
        </main>
    </div>

    <script src="script.js"></script>
</body>
</html>


/* General Styling */
body {
    background-color: #0d0b18;
    color: #fff;
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
    display: flex;
    height: 100vh;
    overflow: hidden; /* Prevents scrolling */
}

/* Layout */
.container {
    display: flex;
    width: 100%;
    height: 100%;
}

/* Sidebar */
.sidebar {
    width: 30%;
    background: #18122B;
    padding: 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    box-shadow: 3px 0px 10px rgba(255, 255, 255, 0.1);
    overflow-y: auto; /* Ensures content fits */
}

/* Align Sidebar Elements from Top */
.sidebar > * {
    width: 100%;
    margin-bottom: 15px;
}

/* Title */
h1 {
    color: #a855f7;
    text-shadow: 0px 0px 8px rgba(168, 85, 247, 0.8);
    font-size: 2em;
    margin-bottom: 5px;
}

p {
    font-size: 1em;
    color: #ccc;
}

/* Textarea */
textarea {
    width: 90%;
    padding: 10px;
    font-size: 1em;
    background: #222;
    color: #fff;
    border: 1px solid #444;
    border-radius: 5px;
    outline: none;
    resize: none;
    transition: all 0.3s ease-in-out;
}

textarea:hover, 
textarea:focus {
    border: 1px solid #a855f7;
    box-shadow: 0px 0px 12px rgba(168, 85, 247, 0.8);
}

/* Generate Button */
button {
    width: 90%;
    padding: 12px;
    font-size: 1em;
    font-weight: bold;
    margin-top: 10px;
    background: #a855f7;
    color: #fff;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: all 0.3s ease-in-out;
}

button:hover {
    background: #9333ea;
    box-shadow: 0px 0px 20px rgba(147, 51, 234, 0.8);
    transform: scale(1.05);
}

/* Loader */
.loader-container {
    display: none;
    margin-top: 15px;
}

.loader {
    border: 5px solid rgba(255, 255, 255, 0.2);
    border-top: 5px solid #a855f7;
    border-radius: 50%;
    width: 40px;
    height: 40px;
    animation: spin 0.8s linear infinite;
    margin: 0 auto;
}

@keyframes spin {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
}

/* Image Section */
.image-section {
    width: 70%;
    background: #0d0b18;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
}

/* Image Container */
/* Image Container */
#imageContainer {
    width: 80%;
    height: 95%;
    background: #222;
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: #fff;
    box-shadow: 0px 0px 12px rgba(255, 255, 255, 0.1);
    overflow: hidden; /* Prevents overflow during scaling */
    transition: all 0.4s ease-in-out;
}

/* Hover Effect */
#imageContainer:hover {
    box-shadow: 0px 0px 20px rgba(168, 85, 247, 0.8); /* Neon glow effect */
    transform: scale(1.02); /* Slight zoom effect */
    border: 2px solid #a855f7; /* Highlighted border */
}

/* Image Styling */
#imageContainer img {
    width: 100%;
    height: 100%;
    border-radius: 10px;
    box-shadow: 0px 0px 12px rgba(255, 255, 255, 0.2);
    transition: all 0.4s ease-in-out;
}

/* Hover Effect for Image */
#imageContainer:hover img {
    transform: scale(1.05); /* Slight zoom-in on the image */
}


/* Responsive Design */
@media (max-width: 768px) {
    .container {
        flex-direction: column;
    }

    .sidebar {
        width: 100%;
        height: auto;
        padding: 15px;
    }

    .image-section {
        width: 100%;
        height: 50vh;
    }

    #imageContainer {
        width: 90%;
        height: 90%;
    }
}



document.addEventListener("DOMContentLoaded", function () {
    const generateBtn = document.getElementById("generateBtn");
    const userInput = document.getElementById("description");
    const imageContainer = document.getElementById("imageContainer");
    const loaderContainer = document.getElementById("loaderContainer");

    generateBtn.addEventListener("click", async function () {
        const description = userInput.value.trim();
        if (!description) {
            alert("Please enter a clothing description.");
            return;
        }

        // Show loader animation
        loaderContainer.style.display = "block";
        imageContainer.innerHTML = "";

        const apiKey = "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"; // Replace with your actual Hugging Face API key
        const apiUrl = "https://api-inference.huggingface.co/models/stabilityai/stable-diffusion-2";

        try {
            const response = await fetch(apiUrl, {
                method: "POST",
                headers: {
                    "Authorization": `Bearer ${apiKey}`,
                    "Content-Type": "application/json"
                },
                body: JSON.stringify({ inputs: description })
            });

            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }

            const imageBlob = await response.blob();
            const imageUrl = URL.createObjectURL(imageBlob);

            // Hide loader and display image
            loaderContainer.style.display = "none";
            imageContainer.innerHTML = `<img src="${imageUrl}" alt="Generated Clothing Image">`;
        } catch (error) {
            console.error("Error:", error);
            loaderContainer.style.display = "none";
            imageContainer.innerHTML = `
                <p style="color: red; font-weight: bold;">
                    ❌ Failed to generate image. Please check your API key or try again later.
                </p>
            `;
        }
    });
});
